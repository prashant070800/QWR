# QWR AI Voice Bot — Python dependencies
# Install with: pip install -r requirements.txt

# ── Web framework & async server ────────────────────────────────────────────
Django>=5.0,<6.0
channels>=4.1,<5.0
daphne>=4.1,<5.0
django-jazzmin>=3.0,<4.0

# ── Config / env ────────────────────────────────────────────────────────────
python-decouple>=3.8

# ── HTTP client (QWR website scraper + STT/TTS REST calls) ──────────────────
httpx>=0.27

# ── Local voice conversion for gTTS MP3 → Exotel PCM ────────────────────────
gTTS>=2.5
pydub>=0.25

# ── LLM providers (install only what you need) ──────────────────────────────
# Gemini (default)
google-generativeai>=0.8

# OpenAI (optional — set AI_PROVIDER=openai)
openai>=1.0

# Anthropic Claude (optional — set AI_PROVIDER=anthropic)
anthropic>=0.30

# ── Google Cloud STT + TTS (optional — set STT_PROVIDER=google / TTS_PROVIDER=google) ──
# google-cloud-speech>=2.0
# google-cloud-texttospeech>=2.0
# google-auth>=2.0
